name: test

on: push

jobs:
  test:
    runs-on:
      - ubuntu-latest
      - ubuntu-24.04-arm
    steps:
      - uses: actions/checkout@v6
      - run: |
          cat /proc/cpuinfo
          lscpu
